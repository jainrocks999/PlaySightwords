import {View} from 'react-native';
import React from 'react';
import Navigation from './components/navigation';
const Root = () => {
  return (
    <View style={{flex: 1}}>
      <Navigation />
    </View>
  );
};

export default Root;
